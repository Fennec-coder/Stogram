<p id="notice"><%= notice %></p>

<h1> Posts </h1>

<tbody>
<% @posts.each do |post| %>
    <tr>
      <td><%= image_tag post.image_url %></td>

      <td><%= post.description %></td>
    </tr>
<%end%>
</tbody>