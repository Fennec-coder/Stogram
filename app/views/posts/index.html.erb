<p id="notice"><%= notice %></p>

<h1> Posts </h1>

<tbody>
<% @posts.each do |post| %>
    <tr>
      <td><%= image_tag post.image_url, :width => 350 %></td>

      <td><%= post.description %></td>
    </tr>
<%end%>
</tbody>